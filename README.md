# 16 Colour Editor

A Base16 colour scheme editor and previewer.

Add or edit colour schemes in [`src/lib/presets.ts`](src/lib/presets.ts), then push to `main` — the site auto-deploys to GitHub Pages.
